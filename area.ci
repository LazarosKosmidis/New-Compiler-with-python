program area
	declare x1,x2,x3;
	declare y1,y2,y3;
	declare a,b,c,answer;
	
	{
		input(x1);
		input(x2);
		input(x3);
		input(y1);
		input(y2);
		input(y3);
		a:=x2/x1*x0;
		b:=y2*y1;
		c:=y3-x3;
		while(x1+x2 <= 0)
		{
			x2:=0;
		};
		if(x+a=0){
			x:=1;
		};
		print(answer);
		print(a+b);
		
	}.